---
layout: base.njk
title: Engagements
---
<div class="mx-blok max-w-3xl pt-32 pb-24">
  <h1 class="text-6xl font-black my-12 text-gray-900 leading-none">Latest Posts</h1>

  <ul>
    {% for post in collections.engagement %}
      <li class="mb-12">
        <span class="text-sm text-gray-700 leading-none">
          Engagement
        </span>
        <h2 class="text-2xl mb-4 font-bold text-gray-900 leading-none">
          <a href="{{ post.url | url }}">{{ post.data.full_title }}</a>
        </h2>
        <span class="text-gray-800 mb-2">
          {{ post.data.date | htmlDateString }}
        </span>
        <p class="text-gray-700 mb-2">{{ post.data.summary }}</p>
        <a class="block" href="{{ post.url | url }}">Read more</a>
      </li>
      {% if not loop.last %}
        <hr class="border-t border-gray-300 mb-12"> 
      {% endif %}
    {% endfor %}
  </ul>
</div>
