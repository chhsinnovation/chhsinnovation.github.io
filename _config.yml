defaults:
  - scope:
      path: ""
      type: "people"
    values:
      layout: "person"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      type: "post"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
  - scope:
      path: ""
      type: "plays"
    values:
      layout: "play"
  - scope:
      path: ""
    values:
      layout: "default"

environment: production

plugins:
  - jekyll-seo-tag
  - jekyll-include-cache

encoding: utf-8

collections:
  pages:
    output: true
    permalink: /:name/
  people:
    output: true
    permalink: /people/:name/
  plays:
    output: true
    permalink: /playbook/:name/

permalink: pretty

exclude:
  - _app
  - node_modules
  - package.json
  - package-lock.json
  - gulpfile.js
  - Gemfile
  - Gemfile.lock
