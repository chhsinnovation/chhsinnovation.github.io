# theme: jekyll-theme-midnight

defaults:
  - scope:
      path: ""
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-seo-tag
  - jekyll-include-cache

encoding: utf-8

collections:
  pages:
    output: true
    permalink: /:path/
  authors:
    output: true

permalink: pretty

exclude:
  - _app
  - node_modules
  - package.json
  - package-lock.json
  - gulpfile.js
  - Gemfile
  - Gemfile.lock
